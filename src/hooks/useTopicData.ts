
// This file is now a wrapper around the refactored hooks in the topics directory
import { useTopicData as useRefactoredTopicData } from './topics/useTopicData';

export const useTopicData = useRefactoredTopicData;
